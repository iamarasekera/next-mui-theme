import { createTheme } from '@mui/material/styles';

// Define your custom theme
const theme = createTheme({
  typography: {
    // Define typography variants and font families here
  },
  // Other theme configurations
});

export default theme;